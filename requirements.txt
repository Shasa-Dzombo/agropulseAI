# FastAPI and ASGI server
fastapi
uvicorn[standard]
pydantic
pydantic-settings

# Database
sqlalchemy
alembic
psycopg2-binary
asyncpg

# Authentication & Security
python-jose[cryptography]
PyJWT  # app/api/auth.py's own JWT stack - do not confuse with the unrelated "jwt" package on PyPI, which shadows the same import name but has a totally different API (no top-level encode/decode)
passlib[bcrypt]
python-multipart
bcrypt<4.1  # passlib's bcrypt backend detection is broken on bcrypt>=4.1 (removed bcrypt.__about__)

# Payment Integration
requests
httpx

# Image Processing
Pillow
opencv-python-headless
numpy
scikit-image  # For advanced image analysis
scipy  # For scientific computing

# Disease Detection APIs
requests  # HTTP client for API calls
urllib3  # HTTP client backend
certifi  # SSL certificates

# AI/ML Integration
boto3  # AWS SDK for Bedrock, SageMaker, S3
azure-ai-vision-imageanalysis  # Azure Computer Vision
openai  # For GPT-based chatbot
anthropic  # Direct Claude API - treatment recommendations, vision diagnosis fallback, farmer chatbot
# tensorflow  # For local AI models (optional)

# Blockchain Integration
web3
eth-account

# Quantum Computing APIs
amazon-braket-sdk
qiskit

# Background Tasks
celery
redis

# Monitoring & Logging
prometheus-client
python-json-logger

# Utils
python-dateutil
pytz
phonenumbers

# Drone orchard survey pipeline
pymavlink  # real MAVLink flight-controller hardware integration

# Testing
pytest
pytest-asyncio
aiosqlite  # in-memory async SQLite DB for tests
