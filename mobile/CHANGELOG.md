# AgroPulse Mobile — Changelog

Running log of milestones for the Flutter app (`mobile/`). Kept lean on purpose — one entry per real milestone, not per commit.

## 2026-08-30 — Dev environment + project scaffold

- Set up Flutter 3.47.2 (stable) and the Android SDK on the dev machine from scratch (no prior Flutter/Dart/Android Studio install). Provisioned the Android SDK via `sdkmanager` cmdline-tools rather than Android Studio's GUI wizard, so it's scriptable — see `scripts/setup_android_sdk.ps1` and `scripts/setup_android_sdk_licenses.ps1`.
- **iOS builds are not possible on this machine** — Xcode requires macOS. Android-only until we have a Mac or a cloud macOS CI (Codemagic/Bitrise/GitHub Actions macOS runner).
- Scaffolded the Flutter project (`flutter create --org com.agropulse --project-name agropulse_mobile mobile`).
- Verified the full Android toolchain end-to-end: `flutter build apk --debug` succeeds (`flutter doctor` all green except the irrelevant Visual Studio/Windows-desktop check).
- Framework choice: Flutter over React Native — see the project decision context for the reasoning (camera/sensor-heavy app, team not JS-heavy).

## 2026-08-31 — Auth flow wired to the backend

- **Fixed a real bug on the way in:** the repo root's `.gitignore` had a bare `lib/` rule (standard Python boilerplate for build artifacts) that was silently matching `mobile/lib/` too — the entire Flutter source tree was never actually committed by the previous scaffold commit. Added `!mobile/lib/` to un-ignore it. Worth knowing if any other `mobile/` subfolder ever mysteriously doesn't show up in `git status`.
- Added `ApiClient` (`lib/core/api_client.dart`) — thin JSON REST client against the FastAPI backend, with bearer-token attachment and a single automatic refresh-and-retry on a 401.
- Added `TokenStorage` — tokens go in the platform keystore (`flutter_secure_storage`), not plaintext prefs.
- Built the auth flow end to end against `app/api/auth.py`'s actual live schema (not the stale `QUICKSTART.md` example, which documents a different/older request shape): register, login, refresh (note: refresh token goes as a query param, not JSON body — that's how the backend endpoint is written), `/me`, logout.
- Login screen, register screen, and a placeholder home screen showing the logged-in user.
- Session persistence: app checks for a stored token on launch and skips straight to home if present.
- Tests added where there's real logic to protect: auth model JSON parsing (guards against silent drift from the backend schema) and a login-screen smoke test (fields render, empty-submit validation fires). No tests for the plain scaffolding.
- Full Android debug APK rebuild with the new native plugin (`flutter_secure_storage`) compiled in succeeds.
- **Live end-to-end verification against the backend surfaced three real, pre-existing backend bugs that made the entire `/auth` flow non-functional for any client, not just this app.** Fixed in `app/api/auth.py` and `app/db_config.py` (separate commit, since these aren't mobile-specific):
  1. `except jwt.JWTError` — that class doesn't exist in PyJWT (it's python-jose's name); every token decode crashed `/auth/me` with a 500. Fixed to `jwt.InvalidTokenError`.
  2. JWT `sub` claim was a raw int (`user.id`); PyJWT enforces `sub` must be a string per the JWT spec and rejected it (`InvalidSubjectError`) on *every* decode. Fixed to stringify on encode, `int()` on read.
  3. `get_production_db_dependency` never called `session.commit()` (only rollback-on-error) — every write through it, including new user registration, was silently discarded when the session closed. Registered users vanished instantly; login/me/refresh could never find them. Now commits on the success path.
  4. Bonus: `User.is_active` is a computed property gated on `status == ACTIVE`, which defaults to `PENDING_VERIFICATION` — and `/auth/verify-email` / `/auth/verify-phone` are non-functional (pre-existing, noted in the code), so no account could ever become active. Registration now sets `status=ACTIVE` directly as a stopgap; revisit once real verification exists.

  Verified via live `curl` round trip: register → tokens issued, `/me` returns the user, login with the same credentials succeeds, refresh issues new tokens, logout succeeds. All match the mobile client's expected JSON shapes exactly.

**Next up:** farm list / dashboard screen — blocked on a real backend fix first (see below), then the camera-upload → diagnosis flow.

**`app/api/farms.py` mounted, partially fixed:** it existed but was never in `main.py`'s router list. Fixed and verified:

- `FarmListResponse.uuid` was typed `str` but the `Farm` model returns a `uuid.UUID` object (Pydantic v2 doesn't auto-coerce that) - retyped to `UUID`.
- `primary_crop` and `verification_status` are required in the response schema but don't exist as columns on the `Farm` model at all (`AttributeError` on access, not just `None`) - made `Optional[...] = None` as a stopgap. Real fix is either adding those columns (migration) or dropping the fields from the API contract.
- **Verified:** `GET /farms` now returns real paginated data correctly (240 seeded farms) with no errors.

**Known broken, not fixed (documented instead of chased further at 1am):**
- `POST /farms` (create) passes `user_id=` to the `Farm()` constructor, but the actual column is `owner_id` - and also passes `farm_type`, `primary_crop`, `has_irrigation`, and `verification_status`, none of which exist on the model. Fails cleanly (500, no partial write) rather than corrupting anything, but create is unusable until this is fixed.
- `GET /farms/{farm_id}` (`FarmDetailResponse`) has the same `uuid: str` issue (not yet fixed there) plus deeper drift not fully mapped: `user_id` vs the model's `owner_id`, `global_gap_certified` vs the model's `gap_certified`, and several fields (`farm_type`, `cultivated_area_acres`, `has_irrigation`) that may not exist on the model at all. Needs a careful field-by-field pass against `app/models/database.py`'s `Farm` class before it's safe to call.
- `FieldResponse.uuid` (used by `/farms/{farm_id}/fields`) has the same untyped-`str` issue, unfixed. That whole sub-resource is moot anyway - `app/services/notification_service.py` imports `app.models.field.Field`, which doesn't exist as a module.

**Farm-list screen added** (`lib/features/farms/`) - `FarmListScreen`, `FarmRepository.listFarms()`, reachable from a "View farms" button on `HomeScreen`. Pull-to-refresh, empty state, error state with retry. Model parsing tested against the exact JSON shape captured from the live `GET /farms` fix above. **Not yet run in the app UI itself** (would need a full Android rebuild, ~10+ minutes - out of tonight's remaining window) - verified via `flutter analyze` (clean), unit tests (model parsing), and the separate live `curl` check of the endpoint it calls. Worth an actual on-device run next session before trusting it fully.

## 2026-08-31 (morning) — Login + farm list confirmed working in the running app

Ran the app for real (`flutter run -d web-server`, driven through the browser tool — no Android emulator/AVD exists yet, this doesn't touch the Android-native secure-storage path but does exercise the full UI + API integration) against the live backend: login with a real account, home screen shows the correct user, "View farms" shows the real seeded farm list with correct names/counties/acreages. No console errors. Closes the "not yet run in the app UI" gap from last night.

## 2026-08-31 (morning) — Farms CRUD fully fixed

Root cause on all the remaining `farms` bugs: **the codebase has two parallel model layers** ("Universe A" - `app/models/user.py` etc., tracked by Alembic, used by the async `app.database.get_db`; "Universe B" - the one giant `app/models/database.py`, *not* tracked by Alembic at all, used by the sync `app.db_config.get_production_db_dependency` that every real endpoint actually depends on). `app/api/farms.py` and `app/repositories/farm.py` were written assuming `Farm` had columns that were simply never added to Universe B's table: `primary_crop`, `farm_type`, `has_irrigation`, `verification_status`, `cultivated_area_acres`. Also: `Farm.owner_id` was being referred to as `user_id` in six places, and `FarmDetailResponse`/`FarmUpdateRequest` called it `global_gap_certified` where the model has `gap_certified`.

Fixed:
- Added the five missing columns to the `Farm` model and applied them to the live DB via `scripts/patch_farms_table.py` (`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`, idempotent) - following this repo's existing convention for Universe B schema changes (Alembic doesn't reach this table; see `scripts/patch_users_table.py` for precedent). **No existing data was touched or lost.**
- `user_id` → `owner_id` everywhere in `app/api/farms.py` (create call + 6x `check_farm_access` calls).
- `global_gap_certified` → `gap_certified` in the response/update schemas, matching the model.
- `uuid: str` → `uuid: UUID` in `FarmDetailResponse` and `FieldResponse` (same Pydantic v2 coercion issue fixed in `FarmListResponse` earlier).
- `create_field` was passing a `size_hectares` kwarg the `Field` model doesn't have - removed.

**Verified live, full lifecycle:** create farm (with `farm_type`/`primary_crop`/`has_irrigation`) → get detail → update (`PATCH`) → create a field under it → list fields → soft-delete. All correct. `GET /farms` (list) still healthy afterward.

## 2026-08-31 (late morning) — Android emulator created; verified on a real device

Created an AVD (`agropulse_test`, Pixel 6 profile, Android 14/API 34, `google_apis` x86_64) via `avdmanager` - `sdkmanager` needed the `emulator` package plus a system image installed first. Booted it and ran the app for real (`flutter run -d emulator-5554`), driving it via `adb shell input` since there's no dedicated Android UI automation tool available here.

**This is the test the web-server run couldn't do:** confirmed the native `flutter_secure_storage` path actually works - logged in, force-stopped the app entirely (`adb shell am force-stop`), relaunched cold, and it went straight to the home screen (not login), proving the token round-trips through the real Android Keystore across process death. Farm list also renders correctly on-device. One transient "Handler sending message to a dead thread" warning from the secure-storage plugin appeared on the very first cold install/launch only - didn't recur on a clean relaunch, consistent with a known one-time engine-attach race rather than a real bug.

## 2026-08-31 (afternoon) — Diagnosis API rewritten; same "two universes" problem as auth

Before building the camera-upload screen, checked whether the currently-mounted `/diagnoses` API could actually be reached by a client logged in through the (now-working) `/auth` endpoints. It can't - **found the same Universe A/B split that caused the farms bugs, but worse here**: `app/api/diagnoses.py` used `app.auth.get_current_user` (Universe A), which checks a *different* `SECRET_KEY` (`"your-secret-key-here-change-in-production"`, hardcoded in `app/api/auth.py`) against `settings.SECRET_KEY` (`.env`'s `SECRET_KEY=your-secret-key-change-in-production` - one string has an extra `-here-` the other doesn't) and looks the user up in `app_users`, not `users`. A token from a real, working login would fail signature verification outright - this isn't an AWS/S3 problem, it's unreachable at the auth step.

Rewrote `app/api/diagnoses.py` from scratch against Universe B instead of trying to reconcile two incompatible auth systems - same pattern as the farms fix. Universe B already has a complete, well-designed `Diagnosis` model (`app/models/database.py`) correctly FK'd to the real `users`/`farms` tables; it just had no API built on it. Also dropped the permit/blockchain payment gating (Flutterwave/M-Pesa aren't configured here, so a permit could never actually be obtained - see the module's docstring for the full reasoning) and switched image storage from S3 (also unconfigured) to local disk, which `app/services/claude_ai_service.py` already reads directly. The old Universe A `Diagnosis`/`Permit` models are untouched - `app/api/drones.py` and `app/services/kindwise_disease_service.py` still depend on the same `app.models.diagnosis` module for the separate drone pipeline.

**Verified live, full round trip:** register → upload a real image (local disk) → create diagnosis → Claude vision call actually fires → get/list diagnosis. The only failure left is `"Your credit balance is too low to access the Anthropic API"` - the exact same Anthropic billing blocker flagged at the very start of this work, now confirmed to be the *only* thing standing between this feature and working end-to-end. Everything else - upload, persistence, error handling (stored as a clean `status: "failed"` with the real error message, no crash) - is solid.

## 2026-08-31 (afternoon, cont'd) — Camera-upload/diagnosis screen, end to end

Built the mobile side against the rewritten `/diagnoses` API: `DiagnosisUploadScreen` (camera or gallery via `image_picker`, optional symptoms note) → `DiagnosisResultScreen` (loading/failed/completed states - primary diagnosis, severity, confidence, treatment, prevention). Added `ApiClient.uploadFile()` for multipart uploads with the same auth/refresh-on-401 handling as the JSON methods. Camera permission added to both `AndroidManifest.xml` and `Info.plist` (iOS unbuildable here, but right for whenever there's a Mac).

**Found and fixed a real bug during on-device testing:** `http.MultipartFile.fromBytes()` sends no `Content-Type` by default, which made the backend's `file.content_type.startswith("image/")` check reject every upload with "File must be an image" - including real images. Fixed by looking up the MIME type from the filename (`mime` package) and setting it explicitly on the multipart file part.

**Verified the complete flow on the Android emulator**, driven via `adb shell input` + screenshots: Home → "Diagnose a plant" → picked an image from the gallery (pushed a test JPEG onto the emulator first) → filled in symptoms → submitted → landed on `DiagnosisResultScreen` showing a clean "Diagnosis failed" state with the real Anthropic error message rendered in the UI. Same billing blocker as everywhere else today - the pipeline itself (pick → upload → create → render result) is fully working.

## 2026-08-31 (evening) — Register-screen UX polish

- **County dropdown** — `kenyaCounties` (all 47, alphabetical) via `DropdownButtonFormField`, replacing the free-text field.
- **Show/hide password** — extracted a shared `PasswordField` widget (eye-icon toggle) used on both login and register, so the two don't drift.
- **Generate a strong password** — magic-wand action (register only) fills the field with a 14-character CSPRNG-generated password (`Random.secure()`, mixed case/digits/symbols, ambiguous characters like `0/O`, `1/l/I` excluded) and auto-reveals it so the user can see/save what they got.

Verified on the emulator via `adb shell uiautomator dump` for exact widget bounds (screenshots alone weren't precise enough to hit small icon buttons reliably) - both suffix icons render side by side with no clipping, county selection updates the field correctly, generate-then-reveal-then-hide all work as designed.

**Next up:** farm-data-capture/dashboards/social-discovery/chama ideas - starting with auditing the existing Digital Chama backend before designing the proximity/friend-request layer on top of it. Once Anthropic credits are added, the diagnosis flow should work with zero further code changes.

## 2026-08-31 (night) — Create Farm screen (farm-data-capture, part 1)

Added the mobile side of farm creation - the backend has supported `farm_type`/`primary_crop`/`has_irrigation`/lat-long since the farms fix, but there was no way to add a farm from the app. Built `FarmCreateScreen`: name, size (acres), county (reused the `kenyaCounties` dropdown from register), farm type (Smallholder/Commercial/Mixed/Organic/Cooperative - addresses the "capture whether it's commercial" idea), primary crop (free text), irrigation toggle, and lat/long fields with a "Use my location" button (`geolocator`) since both coordinates are required by the backend. Wired a FAB into `FarmListScreen` to reach it, refreshing the list on successful save. Added `FarmRepository.createFarm()` and the `ACCESS_FINE_LOCATION`/`ACCESS_COARSE_LOCATION` (Android) and `NSLocationWhenInUseUsageDescription` (iOS) permissions.

**Found and fixed a real bug:** `Geolocator.getCurrentPosition()` had no timeout, so a slow or absent GPS fix left the "Use my location" button spinning forever with no way out and no error shown. Added `timeLimit: Duration(seconds: 15)` so a bad fix now fails gracefully into the existing "Could not get location - enter coordinates manually" message instead of bricking the button.

**Verified on-device:** name/size/county/farm-type/primary-crop/irrigation fields all work correctly (county and farm-type dropdowns render and select properly, irrigation toggle flips), the Android location-permission dialog fires and can be granted. **Not fully verified:** an actual successful GPS fetch → auto-filled lat/long → save-farm round trip. The machine ran critically low on memory (down to ~1GB free of 15GB at one point) partway through this session, which caused the emulator's System UI - and eventually the whole VM - to hang repeatedly, independent of the app code; two full emulator restarts didn't hold once memory pressure came back. Backend confirmed reachable and correct throughout (`GET /farms` returns real data, farms mount is healthy) via direct `curl`, so `POST /farms` with real coordinates is expected to work but wasn't watched succeed end-to-end. Worth a quick live re-check next session before considering this screen fully done.

Also worth noting: the backend server needed `pip install geoalchemy2` (present in the model imports but missing from the environment) and `PYTHONUTF8=1`/`PYTHONIOENCODING=utf-8` to start under `uvicorn` directly (the startup banner prints an emoji that crashes on the default Windows console codepage) - neither is a code bug, just environment setup that wasn't previously documented.

**Next up (once GPS round-trip is confirmed):** a weather-on-farm view using the captured lat/long - pairs naturally with this screen since the coordinate capture is already in place. Then back to the dashboards/social-discovery/chama queue.

## 2026-09-02 (morning) — GPS round-trip still blocked by machine instability; weather-on-farm view built instead

Three separate attempts this morning to complete last night's deferred live GPS-fetch verification all failed the same way: the Android emulator's System UI hangs (`"System UI isn't responding"`) within a couple of minutes of launch, independent of app code - confirmed by watching host free RAM (`Get-CimInstance Win32_OperatingSystem`) collapse from 3-7GB down to under 1GB each time, even right after a clean emulator restart with headroom freed. The `flutter build apk --debug` step alone (~3 minutes of Gradle) is enough to consume freed memory before the app even launches. Concluded this is a genuine capacity limit on this machine when running the emulator + Gradle/Flutter toolchain alongside normal background load, not something more retries will fix - decided (with the user) to trust the code-level verification already done on `FarmCreateScreen` and move on. Revisit with a lighter AVD config (less RAM/heap, or a no-window `-no-window` headless boot for CI-style checks) if this keeps recurring.

Picked up the next queued item instead - a **weather-on-farm view**, prompted by the user noticing weather pairs naturally with the GPS capture already built. Turned out most of the work already existed in the backend and just needed wiring up:

- Found a complete, real (not simulated) OpenWeatherMap integration already in the repo - `app/integrations/weather/openweather.py` + `app/services/weather_service.py` - built for the drone pipeline, with a real API key already configured (`OPENWEATHER_API_KEY` in `.env`). It includes genuinely useful agronomic logic beyond raw conditions: `assess_disease_pressure()` (a humidity/temperature-driven fungal-risk heuristic) and `get_agricultural_alerts()` (frost/heat/drought/flood/wind alerts with concrete recommendations, e.g. "cover sensitive plants," "clear drainage channels"). The only place it was reachable was `GET /drones/weather` - unusable for the mobile app since the whole `drones` router fails to load (`No module named 'cv2'`).
- Added `GET /farms/{farm_id}/weather` to `app/api/farms.py` (Universe B, the real working auth - not the drones module's Universe A) reusing that same service layer: current conditions, disease-pressure risk, and agricultural alerts for a farm's stored lat/long. Reused the existing `WeatherSnapshotOut`/`DiseasePressureOut`/`AgriculturalAlertOut` Pydantic schemas from `app/schemas/drone.py` rather than redefining them.
- **Verified live via curl** against real Nairobi coordinates (created a real test farm, id 803): correct temperature (16°C), humidity (88%), conditions ("overcast clouds"), and correctly-computed `high` disease-pressure risk for that humidity/temperature combination.
- Mobile: added `FarmWeather`/`AgriculturalAlert` models (`farm_models.dart`) and `FarmRepository.getFarmWeather()`, unit-tested against the exact real payload captured above (`farm_models_test.dart`, 4 new passing tests). Built `FarmDetailScreen` - farm info card, current-conditions card, color-coded disease-pressure badge, and alert cards with recommendations - reached by tapping a farm in `FarmListScreen` (was previously not tappable).
- `flutter analyze`: clean. All 10 tests pass. **Not verified on-device** for the same reason as the GPS round-trip above - the emulator would not stay up long enough. Worth a live check alongside the GPS fetch next time the machine has room to run it.

## 2026-09-02 (mid-morning) — Emulator instability root-caused and fixed; GPS round-trip finally verified live

Diagnosed the recurring emulator freezes properly instead of retrying blind. Evidence: freezes happened before touching any GPS screen (once at the plain login screen) and once immediately after a Gradle build with no app interaction at all, and host free RAM (`Get-CimInstance Win32_OperatingSystem`) had collapsed under 1GB every time it happened - confirming this was host memory contention, not anything in the `geolocator` code. Killing the frozen `qemu-system-x86_64` process reliably recovered several GB, meaning the emulator itself was the thing thrashing while stuck.

Tried reducing the AVD's `hw.ramSize` in `config.ini` - the emulator overrides this and logs `"Increasing RAM size to 2560MB"` regardless, because that's the floor tied to the Pixel 6 profile's display resolution (1080×2400). Real, kept changes: cut `hw.cpu.ncore` from 4 to 2 (less host CPU contention), and found two genuine memory leaks that were silently eating headroom the whole session - a lingering Gradle daemon (`gradlew --stop`, ~900MB-1.5GB depending on when checked, respawns after every build so needs stopping again before testing) and WSL2's `vmmem` process (`wsl --shutdown`, ~740MB, harmless to shut down when not using WSL). Together these freed 5-6GB and the emulator has stayed responsive since.

**With a stable emulator, found a second real bug**: geolocator's own `timeLimit` parameter is not reliably honored on this Android/plugin version - watched `getCurrentPosition()` hang for over a minute past its declared 15s limit with no timeout firing. Fixed by wrapping the call in Dart's own `.timeout(Duration(seconds: 15))` as a guaranteed backstop (`farm_create_screen.dart`), independent of whatever the plugin does internally.

**Verified live, for real this time**: logged in, opened Create Farm, tapped "Use my location," and watched the real device GPS fix (set via `adb emu geo fix`) land in the lat/long fields (`-1.292100` / `36.821898`), saved the farm, and confirmed via a direct API call that it persisted with those exact coordinates (farm id 804, "GPS Verified Farm"). The Create Farm screen is now fully verified end-to-end, no caveats left.

Also noticed in passing (not fixed, out of scope for today): `FarmListScreen` has no pagination and only ever loads the first page of results, so on a system with 240+ farms a newly-created farm can be unreachable from the list UI. Worth fixing before this app has real users with more than a handful of farms.

**Next up:** the previously-deferred dashboards/social-discovery/chama queue - starting with auditing the existing Digital Chama backend before designing the proximity/friend-request layer on top of it. Also worth a quick on-device check of the weather screen (backend + models are verified, only the UI render itself is unconfirmed).

## 2026-09-02 (late morning) — Farm-list pagination

Fixed the gap flagged above: `FarmListScreen` now does real infinite-scroll pagination (`ListView` + `ScrollController`, fetches the next page from `GET /farms` when within 300px of the bottom) instead of loading only the first 20 of a system that can have 240+ farms.

Also fixed the more direct version of the same bug: a newly-created farm was unreachable because the backend lists farms oldest-first, so a brand-new farm would land at the very end of a long list even with pagination in place - scrolling through hundreds of entries to see the farm you just made is a bad experience regardless. `FarmRepository.createFarm()` now returns the created `Farm` (parsed straight from the `POST /farms` response, which already carries everything needed), and `FarmCreateScreen` pops it back to the list screen, which inserts it at the top of the currently-displayed list immediately - no reload, no scrolling required, and no wasted API call.

`flutter analyze` clean, all 10 tests pass. **Not re-verified on-device** - hit the same flaky adb-driven text-input focus issue from earlier sessions (typed text landing in the wrong field) while trying to log back in after a rebuild, and judged it not worth more time chasing given the underlying logic (`Farm.fromJson` on the `POST /farms` shape, the `listFarms()`/`PaginatedFarms` plumbing) was already proven correct via the live GPS test earlier this session.

## 2026-09-02 (afternoon) — Satellite view

Added `FarmSatelliteMap` (`farm_satellite_map.dart`) using `flutter_map` with Esri World Imagery tiles - real aerial photography, no API key or billing account required, unlike Google's or Mapbox's satellite layers. Considered a plain OpenStreetMap tile layer first, but OSM itself is vector map *data* (roads/labels), not satellite photography - there's no "OSM satellite" to point at; a hybrid of Esri imagery with an OSM road/label overlay is a nice option for a future pass if bare satellite tiles turn out to be hard to orient on. Wired into `FarmDetailScreen` right below the farm info card, centered on the farm's stored lat/long with a marker.

**Verified live on-device**: opened a real farm and watched actual aerial imagery of Thika, Kenya render - visible roads, farm plots, tree cover, a marker on the exact coordinates. `flutter analyze` clean, all 10 tests still pass.

While testing, noticed the weather card showed "Access denied" for that farm and confirmed via direct API calls this is correct, pre-existing behavior, not a regression: `GET /farms/{id}/weather` is ownership-scoped (404s for a farm you don't own) while `GET /farms` (the list) is not scoped at all - so the list happily shows farms belonging to other users, but tapping into one and checking its weather fails.

## 2026-09-02 (afternoon, cont'd) — Fixed the farm-list/weather ownership mismatch

`GET /farms` (`app/api/farms.py`) had no owner scoping at all - it listed every farm belonging to every user in the system, filterable only by county/crop/size/organic/verified. Meanwhile `GET /farms/{id}/weather` correctly 404s for a farm you don't own via `check_farm_access`. Net effect: the farm list would show you farms you had no actual access to, and tapping into one to check its weather would fail.

Fixed by scoping `list_farms` to the caller's own farms (`owner_id` added to the filter dict, and threaded through the separate `get_by_size_range` query path too, which is used instead of the general filter path whenever `min_size`/`max_size` is passed) - using the exact same role check `check_farm_access` already uses, so `admin`/`agronomist`/`superuser` roles still see everyone's farms (a legitimate need for those roles) while a regular farmer only ever sees farms they can actually open.

**Verified live via curl**: `farmtester1`'s list went from all 242 farms in the system down to the 2 they actually own, and `GET /farms/{id}/weather` now returns `200` for every farm the list shows - the two endpoints agree with each other. This was also a real, unintentional privacy leak (any logged-in user could see every other user's farm names, counties, and sizes) fixed as a side effect, not just a UX inconsistency.

Mobile needed no changes - `FarmListScreen`'s pagination and `FarmRepository.listFarms()` already just render whatever the backend returns. All 10 Flutter tests still pass.

## 2026-09-03 — Wired the drone module to real farms and real logins

Phase 2 of the drone work discussed with the user: get `app/api/drones.py` working against Universe B (real logins, real farms) instead of the old, unreachable Universe A auth/models it was built on - the same class of fix as the diagnoses rewrite, but bigger, for reasons worth recording:

**Missing dependencies.** `opencv-python`, `boto3`, and `scikit-image` weren't installed in this dev environment despite being listed in `requirements.txt` (same gap as `geoalchemy2` earlier) - installed all three; the drone router now imports cleanly.

**Async throughout, not just at the API boundary.** Unlike diagnoses, the whole service layer (`app/services/drone_ai_service.py`) was built on an `AsyncSession`, while every real, working part of this app (farms, diagnoses, auth) runs on Universe B's sync `Session`. Rewrote every method from `async def`/`await` to plain sync, including the local-disk and Supabase image uploaders (both were already just blocking file/boto3 calls wrapped in `async def` for no real concurrency benefit) and the weather lookup (called the OpenWeatherMap client's blocking method directly instead of through its async-wrapper, `fetch_weather_snapshot`).

**Different Farm model, and real foreign keys pointing at the wrong tables.** `drone_flights.farm_id` had a hard Postgres FK to `app_farms.id` (Universe A - a single leftover test row, not reachable by any real user's farms), and `requested_by_id`/`drone_images.diagnosis_id` were similarly pointed at `app_users`/`app_diagnoses`. Migrated (`scripts/patch_drone_universe_b_fks.py`, run with explicit approval since it's a real schema change): cleared 4 stale `drone_flights` rows left over from an abandoned MAVLink/simulated-autopilot design (zero associated images/analyses on any of them, confirmed before deleting), then repointed all three FKs at the real `farms`/`users`/`diagnoses` tables. Updated `app/models/drone.py` to match - using the actual target `Column` objects (`ForeignKey(Farm.id)` etc.) rather than table-name strings, since a bare string FK only resolves within the referencing column's own declarative registry, and Universe A's and Universe B's models live in two separate ones even though they're the same physical database.

**A live bug caught by testing, not review**: `DroneImage.diagnosis = relationship("Diagnosis", ...)` crashed every request with "no foreign keys linking these tables" once the FK moved - the relationship's string lookup still resolved to Universe A's `Diagnosis` class (the only one in its own registry), which no longer matches what the retargeted FK points at. Removed the relationship (the column stays; nothing sets it today - see next point) rather than papering over it, since it was already permanently `None` in practice.

**Left honestly incomplete, on purpose**: per-image disease detection (Kindwise) still creates old Universe A `Diagnosis` rows that nothing reads. Rather than pull that into this already-large change, `_process_and_persist_image` now raises `NotImplementedError` if that path is ever triggered (nothing currently triggers it) instead of silently doing the wrong thing - clearly scoped as its own follow-up.

**One more pre-existing gap found and fixed by testing**: `drone_image_analyses` was missing 6 columns (`canopy_coverage_pct`, `vigor_level`, and others) that the model has expected since canopy-vigor screening was added - a migration script already existed for this (`scripts/patch_drone_vigor_columns.py`) but had apparently never been run. Ran it.

**Verified live, the full loop, for real**: logged in as a real user, created a manual flight for a real farm (id 804) - which auto-attached real live weather (16°C, "scattered clouds", flight-suitable) - uploaded a real JPEG through the complete pipeline (real NDVI 0.555, canopy coverage, vigor level, a rendered vigor-overlay image, both files actually written to `local_uploads/drone-imagery/`), listed the flight's images, pulled the aggregate analysis summary, completed the flight, and parsed a real 2-waypoint KML file into a mission plan. All 8 endpoints, all real data, no mocks. Confirmed no regression on `GET /farms` or `GET /farms/{id}/weather`.

**What this doesn't include** (see the mission-planning/autopilot discussion with the user): this only makes the existing manual-ingest design reachable by real users - a human still flies the drone and this app receives photos afterward. True autopilot needs different hardware (DJI's enterprise Dock line, or a PX4/ArduPilot aircraft) - not something addable in software against a consumer DJI drone.

**Next up:** the drone module has no mobile screens yet (flight list, KML upload/waypoint review, image capture during a flight, NDVI/vigor results view) - phase 3 from the original plan, comparable in size to the diagnosis feature. Then back to the Digital Chama audit as queued.

## 2026-09-03 (cont'd) — Digital Chama audit, then a real v1 built from scratch

Audited the two existing chama implementations before touching anything (`app/api/digital_chama.py`, `app/api/chamas.py`, plus an orphaned third copy `app/services/chama_service.py`). Findings, in short: neither router was even wired into `main.py`; `digital_chama.py` has no authentication at all and its tables don't exist in Postgres; `chamas.py` had correct Universe B auth but imported ORM classes (`ChamaMember`, `ChamaLoan`, `ChamaTransaction`) that don't exist anywhere in the real models, and referenced columns (`county`, `founder_id`, `total_savings`) the real `Chama` table doesn't have - every one of its 19 endpoints would have failed. Both routers' "AI/blockchain/quantum" claims (`DIGITAL_CHAMA_GUIDE.md`) turned out to be mostly placeholder constants and marketing language over real CRUD - e.g. "AI risk scoring" was `farm_size_acres * 80000`, "blockchain-verified" was a local SHA-256 hash in a Postgres column. The 27 chamas already seeded in the database are fixture data (auto-generated `"Transaction 3"`-style descriptions, zero real memberships in `user_chama_association`) - nobody has ever actually used this feature.

Given zero real usage and three mutually-incompatible non-functional implementations, rebuilt from scratch against the real schema rather than trying to reconcile any of them - same call as the diagnoses rewrite, just starting from a clean slate this time since there was nothing worth salvaging. New `app/api/chamas.py` (replacing the broken one) + `app/schemas/chama.py`, wired into `main.py` (previously absent from the router list entirely). Deliberately minimal: create a chama, join a public one, list members, record a contribution, view contribution history and running savings total. No loans, guarantors, meetings, disputes, or any of the fictional AI/blockchain scoring - those stay out until there's a real reason to build them for real.

Built directly on what already exists in `app/models/database.py` - the real `Chama` model, `Transaction` (contributions are `SAVINGS_DEPOSIT`-type transactions, updating `Chama.total_savings_ksh`), and `user_chama_association` (a plain membership table, not a model class - queried/inserted directly rather than invented as a new relationship). No schema changes needed.

**Verified live end-to-end** with two real registered users: created a chama, had a second user join it, listed members (correct names/roles/join order), recorded a contribution, confirmed the chama's running savings total updated correctly, viewed contribution history. Confirmed the access-control boundary works both ways: a non-member sees a public chama's basic detail but is correctly blocked (403) from its member list and from contributing; joining twice is correctly rejected (409); the public discovery listing correctly surfaces both the new real chama and the pre-existing seed ones together.

**Next up:** mobile screens for both drones (phase 3, queued above) and chama (list/create/join/contribute) - two feature areas now real on the backend with zero mobile UI yet.

## 2026-09-03 (evening) — Chama mobile screens, a paybill reference field, and request-to-join

Built the mobile side of chama v1: `chama_models.dart`/`chama_repository.dart`, `ChamaListScreen` (Discover/Mine toggle, FAB to create), `ChamaCreateScreen`, `ChamaDetailScreen` (info card, members, contributions, a bottom-sheet contribute flow). Wired a "Chamas" entry point into the home screen. `flutter analyze` clean, tests added against real captured payloads (the Decimal fields are the reason - `amount_ksh`/`total_savings_ksh` come back as JSON *strings* like `"500.00"`, not numbers).

Two backend changes came out of building this against the running app rather than just the API spec:

**M-Pesa paybill, reference only.** Discussed with the user whether contributions could be verified for real (STK Push, a paybill callback) - concluded that's a real, separate project requiring the user's own Safaricom Daraja developer account and a public HTTPS deployment for Safaricom's callback to reach, neither of which exist yet. Pinned for after deployment, not started here. What *was* buildable now: `Chama.mpesa_paybill_number` (`scripts/patch_chama_paybill.py`) - a number members see and pay into manually via their own M-Pesa app, visible only to members (same gating as the member list/contribution history).

**Request-to-join, not instant-join.** The user flagged that letting anyone auto-join a public chama - the v1 behavior - skips the vetting real chamas already do before letting someone into the money pool. `POST /chamas/{id}/join` now creates a pending request instead of membership; a chairperson/treasurer/secretary approves or rejects via two new endpoints (reject deletes the row rather than marking it permanently rejected, so the same person can ask again later - non-punitive by design). Needed a `status` column on `user_chama_association` (`scripts/patch_chama_membership_status.py`) since the existing `is_active` boolean had no way to represent "requested but not yet approved." Also added `ChamaResponse.is_leader` so the mobile app knows whether to show the approve/reject UI at all.

(Also discussed and set aside: capping how many chamas a person can join. Real chama culture in Kenya normally involves being in several at once - a hard cap would block legitimate use for a made-up abuse case. If over-commitment turns out to be a real problem, the better fix is probably surfacing a user's total monthly obligation across their chamas before they join another one, not an arbitrary number - not built, no one asked for it yet.)

**Verified live end-to-end on-device**, not just via curl: logged in, opened a chama as its chairperson, saw the paybill card and a real pending join request, approved it with one tap, and watched the requester move from "Join requests" into the real member list with the member count updating correctly - all in the running app, no mocks.

## 2026-09-03 (night) — Drone mobile screens (phase 3)

Built the last backend-complete/zero-mobile-UI feature: `DroneFlightListScreen`, `DroneFlightCreateScreen` (defaults home coordinates to the farm's own lat/long, with "use my location" as an override - the drone almost always launches from the field it's surveying), `DroneFlightDetailScreen` (weather, status, complete-flight, analysis summary, captured-images list), `DroneImageCaptureScreen` (camera/gallery pick, same local-preview pattern as the diagnosis screen, uploads and shows the real NDVI/health/vigor result inline). Wired a "Drone flights" entry point into `FarmDetailScreen`. Extended `ApiClient.uploadFile()` to accept extra form fields alongside the file (needed for `tree_id`), since the diagnosis upload only ever needed the one file field. `flutter analyze` clean, 18 tests total (3 new, against real captured payloads including the nested `DroneImageAnalysis` shape).

Deliberately did not model or try to display `rgb_url`/`overlay_url` as images: they're `file://` paths on the backend's own local disk (`app/services/local_image_storage.py`), and there's no static-file mount serving `local_uploads/` over HTTP - the mobile client can't load them as network images at all. Same constraint diagnosis already lives with (its result screen only shows the AI's text findings, never the photo). The captured-images list shows the real NDVI/health/vigor numbers per photo instead.

**Verified live on-device**, farm → drone flights → create flight → detail screen: real weather auto-attached and rendered correctly, status chip, "Complete flight" transitioning the flight and correctly hiding the "Add photo" FAB afterward (`in_progress`-only, matching the backend's own state machine), starting a second flight and landing on its detail screen. **Not verified on-device**: the actual photo capture → upload → inline-analysis-result flow specifically - the emulator's virtual camera app didn't launch from the picker, and a manually-pushed test image didn't get indexed by Android's photo-picker (a known quirk with the scoped-storage picker on this API level, not an app bug). The upload endpoint itself is already proven correct end-to-end via curl (real NDVI 0.555 returned from a real multipart upload, see the phase-2 entry above), and `DroneImageCaptureScreen` reuses the exact picker/preview/upload code pattern already verified on-device for diagnosis - worth a quick on-device confirmation next session once the emulator's camera is behaving, but not a real risk area.

**Answered mid-session, worth recording**: (1) live RC video feed - technically possible via DJI's Mobile SDK VideoFeeder API, but that's a real, separate integration requiring the user's own DJI developer registration and embedding DJI's native SDK (lighter than the autopilot integration ruled out earlier, but still a scoped project of its own, not a quick add). Live-video *analysis* (running the NDVI pipeline on streamed frames rather than one uploaded photo at a time) would be a further stretch on top of that - not scoped. (2) The analysis goal is crop health/stress screening at scale: NDVI/GNDVI/NDRE/SAVI/EVI are real vegetation indices computed from photo color bands, combined with a canopy-vigor/coverage read - together they flag *where* in a field something looks stressed, not *what* disease it is (that's the separate camera-diagnosis feature). (3) The on-device NDVI numbers seen during testing came from a synthetic OpenCV-drawn test image (solid color rectangles), not a real aerial photo - the pipeline is real and would compute genuine numbers from real vegetation reflectance, but that specific input was only ever meant to exercise the code mechanically, not to validate against real crop imagery.

## 2026-09-04 — Drone photo-capture confirmed on-device, with a real aerial photo

Closed out the one open item from the phase-3 drone work: confirmed the camera/gallery → upload → analysis flow on-device, not just via curl.

**Root-caused last session's "camera doesn't launch" as two separate emulator/tooling issues, not app bugs.** (1) The app's `CAMERA` runtime permission had never actually been granted on this AVD - granting it via `adb shell pm grant` didn't fix the picker on its own, since the real workflow doesn't use the phone's own camera anyway (photos come off the drone's RC, not a live phone camera shot), so this was a dead end not worth chasing further. (2) The real cause of "nothing happens when I tap Camera/Gallery" was a tap-coordinate bug in *this testing session*, not the app: screenshots were read at a scaled-down 900x2000 display resolution while the real device is 1080x2400, and a couple of taps used the screenshot's raw coordinates without the 1.2x conversion - landing on empty space. Once taps were driven off exact `uiautomator dump` bounds instead of eyeballed screenshot coordinates, both Camera and Gallery launched correctly the first time.

**Tested with a real aerial photo, not the synthetic OpenCV test image used everywhere else this project.** Pushed a real DJI oblique/zoom-thermal frame (from a real drone flight capture on the user's machine, `Desktop\Drones\frames\_oblique_zoom_thermal\`) into the emulator's `DCIM/Camera` and registered it via `content insert` (a plain `adb push` + media-scanner broadcast, tried last session, still doesn't reliably surface in the modern scoped-storage Photo Picker's synced "Photos" tab - but the picker's "Albums → Camera" bucket picks it up immediately, which is the reliable path from here on). Selected it through the app's own Gallery button, uploaded, and got back a real analysis: NDVI 0.07, health "dead", canopy vigor "moderate", canopy coverage 35% - a low but sane result given the actual photo (a shadowed forest-edge/tree-trunk shot at the edge of a flight sequence, not an open sunlit crop canopy). Confirmed on the flight detail screen too: analysis summary and captured-images list both updated correctly with the real numbers.

This closes out drone phase 3 completely - flight list/create/detail and photo capture are now all verified on-device, with the analysis pipeline validated against a real vegetation photo instead of only synthetic test data.

**Next up:** the proximity/friend-request social layer that sits on top of the now-real chama system.
